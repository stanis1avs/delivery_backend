// Единственный источник имени тестовой базы.
//
// Нужен именно общий модуль: globalSetup выполняется в главном процессе vitest,
// а тесты — в воркерах, и test.env до главного процесса не доходит. Если задать
// имя в двух местах, они разъедутся, и globalSetup выполнит DROP DATABASE
// по рабочей базе.
module.exports = 'delivery_test';
